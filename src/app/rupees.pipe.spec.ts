/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RupeesPipe } from './rupees.pipe';

describe('Pipe: Rupees', () => {
  it('create an instance', () => {
    let pipe = new RupeesPipe();
    expect(pipe).toBeTruthy();
  });
});
